# my-bookshelf

Böcker jag har lyssnat på, med betyg. Statisk sida för GitHub Pages, ingen build.

- Datan ligger i `data/books.json`.
- Öppna sidan med `?edit` för att betygsätta, klicka "Exportera betyg" och checka in den nedladdade `books.json`.
- Lokalt: `npx http-server . -p 8090 -c-1`
